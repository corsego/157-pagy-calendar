<div class="overflow-auto h-96 bg-rose-300">
  <%= turbo_frame_tag :calendar, src: calendar_week_path(date: params[:date] || Date.today), loading: :lazy, target: '_top', data: { turbo_action: 'advance' } %>
<div>
